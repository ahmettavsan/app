import { FormValidatorDates } from './form-validator-dates';

describe('FormValidatorDates', () => {
  it('should create an instance', () => {
    expect(new FormValidatorDates()).toBeTruthy();
  });
});
